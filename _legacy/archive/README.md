# Archiv

Enthält alte/Debug-Dateien, die nicht mehr aktiv genutzt werden:

- `tmp_items.json` – Debug-Ausgabe vom PDF-Text-Extract (mit x/y Positionen)
- `timetable.frompdf.json` – Erster Versuch PDF → JSON (unvollständig)
- `timetable.frompdf.v2.json` – Zweiter Versuch mit verbessertem Parser
- `timetable.json` – Altes JSON-Format (vor der neuen Struktur)
- `pdf-to-timetable.js` – Erster PDF-Parser (ohne Koordinaten-basiertes Parsing)

Aktuell genutzte Dateien:
- `data/timetable.json` – Der aktive Stundenplan
- `tools/pdf-to-timetable-v2.js` – Der aktuelle PDF-Parser
